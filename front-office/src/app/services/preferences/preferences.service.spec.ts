import { PreferencesService } from './preferences.service';

describe('PreferencesService', () => {
  it('should create an instance', () => {
    expect(new PreferencesService()).toBeTruthy();
  });
});
